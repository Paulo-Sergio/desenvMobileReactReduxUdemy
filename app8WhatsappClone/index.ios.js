import React from 'react';
import { AppRegistry } from 'react-native';

import App from './src/App';

const app8WhatsappClone = (props) => (
  <App />
)

AppRegistry.registerComponent('app8WhatsappClone', () => app8WhatsappClone);
